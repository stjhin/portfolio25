/**
 * Shadow core's Post component to ensure our customized Minimal Blog Post UI (with hero banner) is used.
 * This delegates to the Minimal Blog theme's post component, which we already shadowed.
 */
export { default, Head } from "@lekoarts/gatsby-theme-minimal-blog/src/components/post";
