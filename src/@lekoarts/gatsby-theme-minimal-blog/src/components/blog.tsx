// Adapter shadow (deep path): must keep filename as 'blog.tsx' to match theme's internal import path.
// Re-exports Projects to keep UI naming aligned.
export { default, Head } from "../../../../components/projects"
