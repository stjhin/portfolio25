// Adapter shadow: filename must remain 'blog.tsx' so Gatsby theme shadowing picks it up.
// It re-exports our Projects component to keep naming consistent in the UI.
export { default, Head } from "../../../components/projects"
